# Components

No separate component framework files are used by this modal. The root component is embedded in `source/modal/index.php`. Do not invent a replacement component boundary during a zero-logic visual skin pass unless all locked selectors and event contracts remain intact.