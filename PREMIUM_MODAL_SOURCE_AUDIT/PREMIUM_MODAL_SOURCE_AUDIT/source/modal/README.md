# Modal Source

`index.php` is the exact monolithic production source. `resultModal.html`, `swEverywhereModal.html` and `audio-elements.html` are extracted read-only views generated from it for design review. They are not replacement deploy files.